// Modules

// common js modules(node)

// const Circle = require('./circle');

// const c = new Circle(11);

// c.draw(); 






// ES6 Modules(browser)

// import { Circle } from "./circle.js";

// const c = new Circle(11);

// c.draw();



// ES6 Tooling(browser) usually used for browser apps
// transpiler(translator + compiler) = Babel(Tool that converts  modern javascript code into code that all browser understands)
// Bundler(webpack)





