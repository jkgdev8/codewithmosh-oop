// Modules

// common js modules(node)

// const Circle = require('./circle');

// const c = new Circle(11);

// c.draw(); 






// ES6 Modules(browser)

// import { Circle } from "./circle.js";

// const c = new Circle(11);

// c.draw();



// ES6 Tooling(browser) usually used for browser apps
// transpiler(translator + compiler) = Babel(Tool that converts  modern javascript code into code that all browser understands)
// Bundler(webpack) = combines all javascript files into a single file(bundle) it minifies code





// Babel
// npm init -y (creates a file in the folder package.json, its an identifier for an application)
// FLAG --save-dev it will install as development dependencies they will not be part of the app and be part of deployment

// const x = 1;



// const x = 1;





